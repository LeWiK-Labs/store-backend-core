# db.yml
info: { name: DB Health, type: http, seq: 1 }
http: { method: GET, url: "{{baseUrl}}/health/db", auth: inherit }
settings: { encodeUrl: true }
docs: "Verifica conexión a Postgres. Devuelve `db ok`."