info:
  name: Get Stock
  type: http
  seq: 2
http:
  method: GET
  url: "{{baseUrl}}/variants/{{variantId}}/stock"
  auth: inherit
runtime:
  scripts:
    - type: tests
      code: |-
        test("returns 200", function() {
          expect(res.status).to.equal(200);
        });
settings:
  encodeUrl: true
docs: |-
  # Get Stock
  Niveles de stock de una variante del tenant activo.

  ## Respuestas
  - **200** — `{ productVariantId, available, reserved }`
  - **404** `inventory.not_found` — la variante aún no tiene inventario