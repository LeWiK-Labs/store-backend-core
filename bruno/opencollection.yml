info:
  name: LeWiK Store API
  type: collection
headers:
  - name: X-Tenant-Id
    value: "{{tenantId}}"
    enabled: true
docs: |-
  # LeWiK Store API
  Backend de la tienda online (TCG), modular monolith en .NET.

  **Multi-tenant:** desde 4.4 la tienda se resuelve por el DOMINIO de la request
  contra la tabla `stores`. `X-Tenant-Id` sigue heredándose de esta colección
  porque es cómodo acá, pero es una comodidad de desarrollo: está detrás de
  `Tenancy:AllowHeaderOverride`, la app se niega a arrancar con eso prendido
  fuera de Development, y en producción el header no hace nada. Cambiá entre
  "Local · Tenant A/B" para operar como tiendas distintas.

  ## Cómo se identifica la tienda de verdad
  | Host | Resuelve por |
  |---|---|
  | `cardshop.lewik.app` | slug |
  | `panel.cardshop.lewik.app` | slug (se descarta `panel.`) |
  | `cardshop.cl` / `www.cardshop.cl` | dominio propio |
  | `panel.cardshop.cl` | dominio propio (se descarta `panel.`) |

  `panel.` es routing del front, no del backend: resuelve la misma tienda, y qué
  puede hacer quien llama lo deciden las políticas de 4.3. En dev
  `Tenancy:BaseDomain` es `localhost`, así que `cardshop.localhost` es el mismo
  camino. Ver **Health > Tenant by Domain**.

  ## Por dónde empezar (cambió en 4.3)
  1. **Auth > Platform Login** — sesión de operador de LeWiK.
  2. **Platform > Create Store** — crea la tienda; su test deja el id en `tenantId`.
  3. **Auth > Staff Login** — entra al panel de esa tienda.
  4. Recién ahí responde todo lo demás.

  El tenant ya no puede ser un GUID inventado: tiene que ser una tienda que exista
  (`staff_users` tiene FK a `stores`).

  ## La partición
  | Prefijo | Quién entra |
  |---|---|
  | `/platform/*` | PlatformOperator |
  | `/admin/*` | staff de ESA tienda (sesión + tenant match) |
  | `/admin/staff/*` | owner o admin |
  | `/admin/payment-methods/*` | solo owner — decide a qué cuenta llega la plata |
  | `/account/*` | el comprador con cuenta, de ESA tienda |
  | público | catálogo de lectura, stock/drop, checkout, iniciar pago, registro y login de comprador |
  | `/pay/{token}/*` | el invitado, con el token que le dio el checkout |

  Tres poblaciones, tres cookies, tres tablas: `lewik_platform_session`,
  `lewik_panel_session` y `lewik_store_session`. Ninguna credencial sirve en la
  puerta de otra población, y las tres políticas de tienda exigen tenant match.

  ## Las reservas vencen (4.6)
  Un pedido sin pagar retiene su stock (o su cupo de preventa) por
  `Reservations:TtlMinutes` — 30 minutos, también en Development. Después un job lo
  cancela solo y devuelve el stock, así que un pedido que quedó abierto en esta
  colección puede aparecer `Cancelled` sin que nadie lo haya tocado. Cualquier pago,
  aunque sea menor al abono, apaga ese reloj para siempre.

  ## La vidriera y el contador en vivo (4.7)
  `GET /storefront` devuelve catálogo + disponibilidad + topes en una sola llamada
  (ver la carpeta **Storefront**), y esos números se mueven solos por SignalR. El hub
  no se puede probar desde Bruno; para verlo, la API sirve `/signalr-test.html` en
  Development. Los grupos llevan el tenant, que se resuelve del host al conectar, así
  que una tienda nunca recibe las novedades de otra.

  Todo lo que cambia estado bajo `/admin` o `/platform` necesita además el header
  `X-Requested-With: LeWiKPanel` (capa extra sobre SameSite=Lax y CORS estricto).
  Las requests de esta colección ya lo llevan.